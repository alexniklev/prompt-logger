{
  "description": "Prompt Logger MCP server – exposes a SavePrompt tool that writes prompts as markdown files (with YAML frontmatter) into a git repository, commits, and pushes.",
  "name": "io.github.alexniklev/prompt-logger",
  "packages": [
    {
      "registry_name": "nuget",
      "name": "an.prompt-logger",
      "version": "1.0.0",
      "package_arguments": [],
      "environment_variables": [
        { "name": "REPO_PATH", "description": "Path to an existing git working copy (default: current working directory)." },
        { "name": "PROMPTS_FOLDER", "description": "Folder (relative to repo root) where prompt files are stored (default: prompts)." }
      ]
    }
  ],
  "repository": {
    "url": "https://github.com/alexniklev/prompt-logger.git",
    "source": "github"
  },
  "version_detail": {
    "version": "1.0.0"
  }
}
